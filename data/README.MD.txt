Store JSON and CSV files here to be loaded into an org on creation.

Relevant SFDX commands:
	SFDX force:data

Add -h to the end of the above commands for more details.

Another option includes storing the files as static resources and loading them in via Apex.
 
The best option is to include an apex framework for creating dynamic test data, this option requires the most investment though.